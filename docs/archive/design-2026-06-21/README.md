# 归档 · 2026-06-21 设计稿(Carnet de classe 初版)

> **已封存,不是契约。** 2026-09-02 自 `line_math/design/` 移入(此前从未入库,是孤本)。
> 现行视觉裁定 = `docs/design-constitution.md` + `docs/aesthetic-profile.md`,现行设计稿 = `docs/handoff-2026-08-20/`。
> 本目录的 `START-HERE.md` / `HANDOFF.md` 里的"逐像素硬契约、一次只做一页"等指令**对当前代码不再生效**,不要照着执行。

仍有参考价值的部分:
- `spec/Vocabulary.md` + `Vocabulary.dc.html`:背词器六题型与 TCF/TEF/DELF B2–C1 选词口径的原始出处,`src/lib/exerciseGenerator.js` 的题型设计源于此。
- `HANDOFF.md` 的"文案规范"(`中文 · Français`、零英文 UI、零技术黑话)在 08-20 宪法里延续。

已被现状推翻的部分:目标仓 `MathClassWebsite-public`(已不存在)、主色 `#8b0000`(现为 `#7f302b`)、Atelier / SolanaWitness / PageLoading 三页(线上已重定向或退役)。
